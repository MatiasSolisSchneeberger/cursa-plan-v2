---
export const prerender = false

import { supabase } from '../db/supabase';
import Layout from '@layouts/Layout.astro';
import HeroTitle from '@sections/HeroTitle.astro';
import Carreras from '@sections/Carreras.astro';
const { data, error } = await supabase.from("carreras").select(`id, name, emojie`);
console.log(data)
---

<Layout title="CursaPlan | Plan de estudios" pagina='Inicio'>
	<ul>
		{data && data.length > 0 ? (
		  data.map((entry) => (
			<li>{entry.id}{")"} {entry.name} - {entry.emojie}</li>
		  ))
		) : (
		  <li>No se encontraron carreras.</li>
		)}
	  </ul>

	  
	<HeroTitle />
	
	<!-- <div class="grid grid-cols-3">
		<div class="bg-primary-500-v2 text-primary-500 size-16">primary</div>
		<div class="bg-secondary size-16">secondary</div>
		<div class="bg-tertiary size-16">tertiary</div>
	</div> -->
	<!-- <div  class=" grid grid-cols-2 gap-2 columns-sm">

		<Button href="/" type="primary">
			primary
		</Button>
		<Button disabled type="primary">
			primary
		</Button>
		<Button href="/" type="secondary">
			secondary
		</Button>
		<Button href="/" type="secondary">
			secondary
		</Button>
		<Button href="/"type="tertiary">
			tertiary
		</Button>
		<Button href="/" type="tertiary">
			tertiary
		</Button>
		<Button href="/"type="text">
			text
		</Button>
		<Button href="/" type="text">
			text
		</Button>
		<Button href="/"type="danger">
			danger
		</Button>
		<Button href="/" type="danger">
			danger
		</Button>
		<Button href="/"type="warning">
			warning
		</Button>
		<Button href="/" type="warning">
			warning
		</Button>
		<Button href="/"type="success">
			success
		</Button>
		<Button href="/" type="success">
			success
		</Button>
		<Button href="/"type="info">
			info
		</Button>
		<Button href="/" type="info">
			info
		</Button>
	</div> -->

	<Carreras />
</Layout>
